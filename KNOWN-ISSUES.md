# 已知问题（Known Issues）

记录已定位但暂不修复的问题：现象、已确认的根因、不可行的修法，以及将来动手前必须知道的前提。
每条问题都标注证据来源，便于后续复核。

## AI 续写：偶发草稿缺少章节标题

**状态**：已知，暂不修复（2026-09-12 评估）

**现象**

多章续写后，个别草稿在管理端列表里显示为「待命名章节」，其余章节标题正常。
2026-09-12 对《我在斗气大陆与美女双修》续写 10 章的实测中，批次 `continue_mtxq3bnt_tmsq2x`
出现 1 例（第 5 章草稿 `aigen_mtxq58u6_f0faa57c`），比例约 1/10。

**已确认的根因：不是代码缺陷，是模型偶发不遵守输出格式**

生成流程要求模型「第一行输出本章标题、第二行空行、第三行起正文」。`parseContinuationTitle`
（`api/src/services/ai/writing.ts`）负责按该格式提取首行标题：标题存入 `params_json.draftTitle`，
正文剥掉标题行后落库。

10 章里 9 章模型正常输出了标题行并被正确解析；第 5 章模型直接从叙述句开写，首行为
「魂渊遁走，皇都上空的浓云散了大半……无人觉得温暖。」（61 字、以句号结尾）。

解析器对此返回 `{ title: '', body: 原文 }`，这是**正确行为**——该行确实是正文，不是标题。
缺标题时 `draftTitle` 被省略，草稿照常入库，发布时由发布路径按现有章节序号推算标题。
该章正文完整（98 行、约 3500 字、结尾正常）。

前端在 `draftTitle` 为空时兜底显示「待命名章节」。这个文案恰好与运营侧
`ai_settings.writingSystemPrompt` 第 6 条禁止的占位词同名（该条禁止模型输出
「待命名章节」等前缀或占位词），两者撞名，容易让人误判为生成失败。

**验证记录**

用线上真实 prompt（1655 字 system + 19KB user）单独复现时，模型严格遵守格式，
输出 `第四十七章 早安之后有客来` + 空行 + 正文。说明提示词本身有效，问题出在批量调用的
稳定性上，属概率性问题，10 次约 1 次。

**不可行的修法（已实测，勿重复尝试）**

自动补标题会让语义错位。曾尝试在 `draftTitle` 缺失时用 `batchIndex` 生成「第 N 章」兜底，
被端到端测试当场拦截：

```
expected [ '第 1 章', '第 3 章' ] to deeply equal [ '第 3 章', '第 4 章' ]
```

原因是 `batchIndex` 是**本批批次内序号**（1、2、3），不是**全书章节序号**。
`draftTitle` 在发布路径上是**高优先级字段**，会覆盖系统的序号推算，写入假章号会导致
发布出来的章节序号错乱。该改动已完整撤销，`writing.ts` 保持原状。

另一个不推荐的方向是放宽 `parseContinuationTitle` 的判定条件。现有条件
（首行 2–30 字、不以句末标点结尾、紧跟空行）正是挡住第 5 章那种正文首句的依据；
放宽会引入把真正正文首句误判为标题、进而切掉正文一刀的风险，负收益。

**将来若要改善体验，只考虑这两条（都不动后端生成逻辑）**

- 调整前端 `draftTitle` 为空时的兜底文案，避免与禁用占位词撞名，或直接留空。
- 在管理端草稿列表给缺标题的条目加「补标题」按钮，用户点击时才调
  `generateWritingTitles`（`api/src/services/ai/writing.ts`），不做自动触发。
  该函数已存在且可用，按正文提炼 1–3 个候选标题。

**相关回归保护**

`api/src/services/ai/writing.test.ts` 中两条用例锁定了解析器的既有行为，改动解析逻辑时
必须同时通过：

- 「模型漏输出标题行时返回空标题，不误剥正文首句」
- 「以句末标点结尾的短首行仍不当作裸标题」

## AI 续写：网关层内容过滤命中时，拒答文案被当作正常草稿落库

**状态**：已知，暂不修复（2026-09-12 记录）

**现象**

上游网关命中内容过滤时返回固定短文案（实测 13 字「你好，我无法给到相关内容。」），
`finish_reason=content_filter`。该文案会作为正常草稿写入 `ai_generations`，在管理端与真实
草稿混在一起，需要人工识别。

**相关代码位置**

`api/src/services/ai/client.ts` 第 182 行调用 `detectStructuredContentRefusal`，命中即抛
`AiError('invalid', ..., 422)`。但模型**自主**拒答（`finish_reason=stop`，拒答文本写在
`reasoning_content` 或正文里）不走这条路径，会静默落库。

**可考虑的方向**

引入短文本拒答识别 + 写入前守卫，把拒答转成可重试状态，而不是落成草稿。
与上一条一样，属体验优化，未排期。

**注意与上一条的区别**

这里说的是**上游过滤器**（`content_filter`）和**模型自主拒答**（`stop` + 短文本）两层。
提示词层只能影响后者。前者由网关策略决定，提示词改动覆盖不到。
