# 已知问题（Known Issues）

记录已定位但暂不修复的问题：现象、已确认的根因、不可行的修法，以及将来动手前必须知道的前提。
每条问题都标注证据来源，便于后续复核。

## AI 续写：偶发草稿缺少章节标题

**状态**：已知，暂不修复（2026-09-12 评估）

**现象**

多章续写后，个别草稿在管理端列表里显示为「待命名章节」，其余章节标题正常。
2026-09-12 对《我在斗气大陆与美女双修》续写 10 章的实测中，批次 `continue_mtxq3bnt_tmsq2x`
出现 1 例（第 5 章草稿 `aigen_mtxq58u6_f0faa57c`），比例约 1/10。

**已确认的根因：不是代码缺陷，是模型偶发不遵守输出格式**

生成流程要求模型「第一行输出本章标题、第二行空行、第三行起正文」。`parseContinuationTitle`
（`api/src/services/ai/writing.ts`）负责按该格式提取首行标题：标题存入 `params_json.draftTitle`，
正文剥掉标题行后落库。

10 章里 9 章模型正常输出了标题行并被正确解析；第 5 章模型直接从叙述句开写，首行为
「魂渊遁走，皇都上空的浓云散了大半……无人觉得温暖。」（61 字、以句号结尾）。

解析器对此返回 `{ title: '', body: 原文 }`，这是**正确行为**——该行确实是正文，不是标题。
缺标题时 `draftTitle` 被省略，草稿照常入库，发布时由发布路径按现有章节序号推算标题。
该章正文完整（98 行、约 3500 字、结尾正常）。

前端在 `draftTitle` 为空时兜底显示「待命名章节」。这个文案恰好与运营侧
`ai_settings.writingSystemPrompt` 第 6 条禁止的占位词同名（该条禁止模型输出
「待命名章节」等前缀或占位词），两者撞名，容易让人误判为生成失败。

**验证记录**

用线上真实 prompt（1655 字 system + 19KB user）单独复现时，模型严格遵守格式，
输出 `第四十七章 早安之后有客来` + 空行 + 正文。说明提示词本身有效，问题出在批量调用的
稳定性上，属概率性问题，10 次约 1 次。

**不可行的修法（已实测，勿重复尝试）**

自动补标题会让语义错位。曾尝试在 `draftTitle` 缺失时用 `batchIndex` 生成「第 N 章」兜底，
被端到端测试当场拦截：

```
expected [ '第 1 章', '第 3 章' ] to deeply equal [ '第 3 章', '第 4 章' ]
```

原因是 `batchIndex` 是**本批批次内序号**（1、2、3），不是**全书章节序号**。
`draftTitle` 在发布路径上是**高优先级字段**，会覆盖系统的序号推算，写入假章号会导致
发布出来的章节序号错乱。该改动已完整撤销，`writing.ts` 保持原状。

另一个不推荐的方向是放宽 `parseContinuationTitle` 的判定条件。现有条件
（首行 2–30 字、不以句末标点结尾、紧跟空行）正是挡住第 5 章那种正文首句的依据；
放宽会引入把真正正文首句误判为标题、进而切掉正文一刀的风险，负收益。

**将来若要改善体验，只考虑这两条（都不动后端生成逻辑）**

- 调整前端 `draftTitle` 为空时的兜底文案，避免与禁用占位词撞名，或直接留空。
- 在管理端草稿列表给缺标题的条目加「补标题」按钮，用户点击时才调
  `generateWritingTitles`（`api/src/services/ai/writing.ts`），不做自动触发。
  该函数已存在且可用，按正文提炼 1–3 个候选标题。

**相关回归保护**

`api/src/services/ai/writing.test.ts` 中两条用例锁定了解析器的既有行为，改动解析逻辑时
必须同时通过：

- 「模型漏输出标题行时返回空标题，不误剥正文首句」
- 「以句末标点结尾的短首行仍不当作裸标题」

## AI 续写：网关层内容过滤命中时，拒答文案被当作正常草稿落库

**状态**：已知，暂不修复（2026-09-12 记录）

**现象**

上游网关命中内容过滤时返回固定短文案（实测 13 字「你好，我无法给到相关内容。」），
`finish_reason=content_filter`。该文案会作为正常草稿写入 `ai_generations`，在管理端与真实
草稿混在一起，需要人工识别。

**相关代码位置**

`api/src/services/ai/client.ts` 第 182 行调用 `detectStructuredContentRefusal`，命中即抛
`AiError('invalid', ..., 422)`。但模型**自主**拒答（`finish_reason=stop`，拒答文本写在
`reasoning_content` 或正文里）不走这条路径，会静默落库。

**可考虑的方向**

引入短文本拒答识别 + 写入前守卫，把拒答转成可重试状态，而不是落成草稿。
与上一条一样，属体验优化，未排期。

**注意与上一条的区别**

这里说的是**上游过滤器**（`content_filter`）和**模型自主拒答**（`stop` + 短文本）两层。
提示词层只能影响后者。前者由网关策略决定，提示词改动覆盖不到。

## AI 续写：R18 密度低于原作（已部分修复，存在天花板）

**状态**：部分修复，剩余差距判定为情节结构问题，不再投入（2026-09-12 评估）

**现象**

对成人向作品续写时，露骨内容密度显著低于原作。以《我在斗气大陆与美女双修》
（`novel_ms3y595y_etmihj`）实测，原作最近 15 章为 23.0 次/千字（露骨词命中），
续写仅 2.2 次/千字，相差 10 倍。原作最近 20 章中 0 命中章节数为 0，续写 10 章中
6 章低于 5 次、2 章为 0。

**根因（三层，均已实测确认）**

第一层，`intimacyWeight` 的语义被写成了「叙事强调程度，不是固定字数或段落百分比；
不为凑比例强行加入场景」，并被描述为「服务于人物关系与剧情推进」。模型据此把
成人内容理解为可选增强项。原作的叙事模型是「章节事件即性事件」，不是「剧情 + 可选情色」。

第二层，数据库 `ai_settings.writingSystemPrompt`（1655 字）与本书题材直接冲突：
第 8/9/10 条禁止对强迫、下药、迷醉、睡眠等情境作直接描写，而原作主线正是这些
（章节标题即为「强奸雅妃」「睡奸萧媚」「给云韵下春药」）。这批规则不解除，
user 层的任何量化要求在 system 层都会被压制。

第三层，同一份 system prompt 中的篇幅约束（「权重只表示叙事强调程度，不代表固定
字数、段落数量或出现频率。不得为了满足权重而强行加入亲密场景」）会抵消任务参数里的
场景数量与篇幅占比要求。实测这一句是密度最强的抑制源。

**已实现的修复**

新增 `WritingContentPreferencesV1.consentRuleTier`（`default` | `fictional_nonconsent`），
缺省为 `default` 以兼容旧客户端：

- `default`：保留全部同意规则与篇幅约束，适用于绝大多数作品。
- `fictional_nonconsent`：仅用于全部角色均为成年人、且原作本身即以此类情节为主线的
  虚构成人向作品。放宽写法限制与篇幅安排，并在 system 层注入覆盖声明
  （`writing-prompt.ts`），确保能盖过 DB prompt 的对应条款。「角色均为成年人」前提不放松。

刻意**未**按 `novels.categories` 自动推断档位。本书 categories 已是 `["h","np"]`，
自动推断本可实现，但那会让任何被打上成人标签的书静默失去同意约束，副作用不可审计；
改为由调用方显式传参。

**实测数据（同一本书、同一起点、6 章批次）**

| 配置 | 密度（次/千字） |
| --- | --- |
| 原作基线 | 23.0 |
| 改动前线上 | 2.2 |
| 仅加强措辞（严格档） | 3.5 |
| 题材分级（放宽同意规则） | 8.9 |
| 题材分级（再放宽篇幅约束） | 5.5（同类章 6.5） |

最后一行低于上一行**不是回退**：该批抽到连续战斗章节（云州截杀、断魂峡伏杀），
其中伏击章密度 0 是正确的——那一章本就是军事情节。两批第 1 章密度分别为 12.1 与 10.7，
说明提示词效果相当，批次间差异来自情节走向。

**剩下来的差距为什么不再修**

原作 23.0 的高密度来自其情节结构：「晋升斗皇」与「山洞交媾」是同一场戏，
「截胡萧炎」与「带走美杜莎」是同一场戏——战斗、修炼、权力争夺全部通过性场景推进。
续写在战斗章节就写战斗（第 4 章运筹、伪装、埋伏，内容质量扎实，密度 0）。
这是**情节设计**层的差异，提示词层改不动。继续加硬指令只会换来机械堆砌，
且已观察到副作用（模型开始模仿原作的繁体杂糅文本，出现「斷魂峽」这类标题）。

**如果确实需要高密度章节**

正确做法是在**作者指令**（`instruction` / `writingBrief`）里写明具体场景，而不是靠全局提示词压制。
实测对比（同书、同起点）：

| 配置 | 密度（次/千字） | 章节标题走向 |
| --- | --- | --- |
| 题材分级（放宽档），不写指令 | 8.9 / 5.5 | 随机，可能整批抽到战斗章 |
| 题材分级 + 指令写明「本章写一场完整的双修戏」 | 10.0 | 四章全部围绕双修 |

指令写法示例（实测有效）：

```
本章写一场完整的双修戏：苏越与夭夜在密室中双修，从试探到完全结合，
写清双修如何提升修为、阴阳帝炎如何流转、两人身体与情绪的变化。
这场双修本身要推动剧情（修为突破引发新的局面），不要写成剧情之外的
插曲，也不要用概述带过。
```

关键差别不在密度数字，而在**情节结构**：加了指令后，模型会把性场景嵌入剧情主线
（苏越坦白身份 → 约定对付云山 → 入地底密室疗伤 → 双修），与原作「章节事件即性事件」
的结构一致；不加指令时，模型对情节方向自由发挥，抽到「云州截杀」「断魂峡伏杀」
这类章节就是纯动作戏，密度 0 属正确结果而非缺陷。

结论：**密度由情节方向决定，情节方向应由作者指令指定，不应靠全局提示词压制。**

密度本身也不是好指标：原作 23.0 中包含大量「肏/操」的字面重复，可读性并不优于 10.0 的版本。

**对非 R18 作品无影响的隔离验证**

改动上线后实测确认题材分级不会波及普通作品。以古言《春台记事》
（`novel_mse1seq6_l58iq4`，原作末 3 章密度 0.21）为样本，同书同起点：

| 模式 | 续写密度 | 结果 |
| --- | --- | --- |
| `adultContentMode: off`（非 R18 默认） | 0.00 /千字 | 3 章全部 0 命中，文风与情节延续原作 |
| `adultContentMode: explicit`（对照） | 0.23 /千字 | 与原作 0.21 基本一致，未出现露骨词堆砌 |

隔离由三层保证：`formatWritingContentPreferences` 在 `adultContentMode === 'off'` 时提前
返回，只输出三行中性文本，不进入任何新增逻辑；题材分级声明仅在
`consentRuleTier === 'fictional_nonconsent'` 时注入 system 层；`consentRuleTier` 缺省为
`default`，旧客户端不带该字段时行为与改动前一致。

对照组的 `explicit` 模式同样未被污染：模型会尊重原作自身的尺度，`春台记事` 是古言，
亲密戏便用「帐顶的暗纹跟着一闪，像水底的鳞」这类意象处理，不因开关开启而改变文风。
这再次印证密度由**作品题材与作者指令**决定，而非开关本身。

另注：`off` 模式下校验器仍会补上 `consentRuleTier: 'default'` 并冻结进任务参数，
这是预期行为（用于审计），不影响生成结果。

## AI 续写：多章批次的 instruction 会被原样复制到每一章

**状态**：已知，未修复（涉及所有多章续写的行为，需单独评估）

**现象**

一次生成 N 章时，作者填写的 `instruction` 会原样发给每一章，只在末尾追加
「这是续写的第 N 章，共 N 章」。若指令写的是单章事件（例如「本章写新婚夜」），
模型收到 N 次相同要求，只能把同一场戏重写 N 遍。

实测（`chapterCount: 3`，指令为一段新婚夜场景描述）：

| 章 | 标题 | 开头 |
| --- | --- | --- |
| #1 | 新婚夜 | 夭夜换下那身白灰色**紧身**战袍时，指尖在束腰的银扣上停了一息…… |
| #2 | 新婚夜 | 夭夜换下那身白灰色战袍时，指尖在束腰的银扣上停了一息…… |
| #3 | 新婚夜 | 夭夜换下那身白灰色战袍时，指尖在束腰的银扣上停了一息…… |

三个标题完全相同，第 2、3 章正文开头逐字重复。对照：同一条指令改为
`chapterCount: 1`，输出为完整独立的单章，开头细节明显更丰满
（用「锁子甲领口烙出的浅红压痕」交代夭夜连月领兵的处境）。

**根因**

`writing.ts` 的续写循环里，`instruction` 由 `opts.instruction` 加一句
「这是续写的第 N 章」拼成，逐章传入 `generateWriting`，没有按章拆分机制。

**当前规避方式**

单章指令配 `chapterCount: 1` 使用。需要一次多章时，把指令写成明确分章的递进式描述
（「第 1 章写…；第 2 章写…」），但这有被模型揉进同一章的风险。

**将来可做的修法**

按章拆分指令（约定分隔符或结构化数组），或让第 2 章起不重复传入单章指令、
改为由前文自然承接。前者更明确，后者更省事但会削弱作者对后续章节的控制。

## 功能：情节方向推荐（`/api/ai/writing/plot-suggestions`）

**状态**：已实现（2026-09-12）

**用途**

续写指令留空时模型会自由发挥，情节走向随机（实测同书同起点的两批无指令续写，
密度分别为 8.9 与 4.1，后者整批滑向探墓打怪线）。此接口基于最近章节上下文
推荐若干条可直接用作续写指令的情节方向，解决「想不出写什么」。

**请求**

```json
{ "novelId": "...", "afterChapterId": "...", "focus": "想写感情升温的日常互动", "contentPreferences": { "version": 1, "adultContentMode": "explicit", "intimacyWeight": "high", "adultCharactersConfirmed": true, "consentRuleTier": "fictional_nonconsent" } }
```

`afterChapterId` 缺省用最后一章；`focus` 可为空，填写后建议会围绕该侧重点；
`contentPreferences` 走与续写相同的校验，缺省按关闭处理。

**实现要点**

- 输入复用 `loadContinuationContext`（最近 3 章 + 作品题材与简介），
  因此建议紧扣原文已出现的人物、设定与未完成线索，不凭空发明世界观。
- `contentPreferences.adultContentMode === 'explicit'` 时提示词改为要求
  **至少 2 条以成人亲密场景本身为主体情节**（场景是这一章的主要事件、
  剧情在其中推进或转折），并要求直接使用露骨表述、不得用含糊措辞回避。
  未开启时维持原来的五类剧情维度，不会给出成人向方向。
- 开启成人模式时，system 层与要求列表都声明「参与角色必须全部是成年人」，
  并明确禁止推荐涉及未成年、幼态体型或年龄模糊化的方向。
- `parsePlotSuggestions` 同时容忍 JSON 对象数组、纯字符串数组与带序号的纯行列表
  三种模型输出形态。
- 一次调用约 8300 prompt + 550 completion token，成本可忽略。

**实测输出（《我在斗气大陆与美女双修》，传 explicit 参数）**

5 条全部以成人场景为主体，且都落在原作已埋的伏笔上：

1. 夭夜以口交与骑乘作「契约仪式」与订盟礼，借双修稳固斗皇星阶
   （朝堂政治线，关系从被强迫转为主动结盟）
2. 本体潜入云岚宗，在云韵寝殿以幻化之身行事，床席间套出云山与魂殿的底细
   （情报线，埋云韵背弃伏笔）
3. 与萧玉、萧媚重逢后同夜行事，事后借生生不息之力助两人冲击斗皇瓶颈
   （回收萧家失踪伏笔，壮大后宫战力）
4. 与美杜莎一边交媾一边商议肃清内奸，事后当众重掌王权
   （蛇人族内乱线，牵出魂殿在魔兽族群的渗透）
5. 冒充夭月接近云岚宗女弟子，在过程中偷学斗技并甄别下一位双修对象
   （新角色与新斗技来源）

**约束验证**：加入成年前提前，曾生成含「萝莉体型」表述的方向；加入后同样剧情位置
改写为不含年龄暗示的表述，5 条全部通过禁区词检查。
