# ── 依赖 ──
node_modules/

# ── 构建产物 ──
dist/
coverage/

# ── 环境与密钥 ──
.env
.env.*
!.env.example
*.key
*.pem
.dev.vars

# ── 日志 ──
*.log

# ── 本地数据 ──
data/
.tmp/

# ── OS / 编辑器 ──
.DS_Store
Thumbs.db

# ── Impeccable 运行时产物（设计系统文档 DESIGN.md / design.json / critique 保留）──
.impeccable/hook.cache.json
.impeccable/live/
web/.impeccable/

# ── 临时截图 ──
web/screenshots/
